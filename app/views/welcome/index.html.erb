<h1>Hello, Rails!</h1>
<%= link_to '[Login]', '/auth/bnet' %>

<%= select("name", "zone", @bosses, {include_blank: 'None'}) %>

<table>
<% @characters.each do | character | %>
  <tr>
    <td><img src="<%= character.portrait %>"></td>
    <td><%= character.name %></td>
    <td><%= character.ilvl %></td>
  </tr>
<% end %>
</table>
